[%autowidth.stretch, cols="h,a"]
|===
|Base package
|`io.gwanmun.web`
|Spring components
|_Controllers_

* `i.g.w.GatewayController`
* `i.g.w.LedgerController`
* `i.g.w.MessageController`
* `i.g.w.ReconciliationController`
* `i.g.w.ResilienceController`
* `i.g.w.ResolutionController`
* `i.g.w.TransactionHistoryController`

_Services_

* `i.g.w.ReconciliationService`

_Others_

* `i.g.w.ReconciliationScheduler`
* `i.g.w.WebFilterConfig`
|Bean references
|* `i.g.g.GatewayService` (in gateway · 관문(필터 체인))
* `i.g.l.TransactionLedger` (in ledger · 거래 원장)
* `i.g.l.TransactionIdGenerator` (in ledger · 거래 원장)
* `i.g.l.IdempotencyService` (in ledger · 거래 원장)
* `i.g.m.MessageRegistry` (in message · 전문 코덱)
* `i.g.l.ReconciliationLog` (in ledger · 거래 원장)
* `i.g.c.SettlementClient` (in core · 계정계 연동)
* `i.g.g.TransactionResolutionService` (in gateway · 관문(필터 체인))
* `i.g.c.CoreBankingClient` (in core · 계정계 연동)
* `i.g.c.TransactionHistoryClient` (in core · 계정계 연동)
|===
