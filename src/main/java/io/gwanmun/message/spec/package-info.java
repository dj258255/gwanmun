/**
 * 전문 레이아웃 스펙(@Field·타입·오프셋). core 모듈이 응답 전문 길이를 스펙에서 읽어 오므로,
 * 명명된 인터페이스("spec")로 공개한다.
 */
@NamedInterface("spec")
package io.gwanmun.message.spec;

import org.springframework.modulith.NamedInterface;
